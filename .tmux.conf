# mouse support
set -g mouse on

# use vi keys
set -gw mode-keys vi

# 256 color support
set -g default-terminal "screen-256color"
set-option -ga terminal-overrides ",xterm-256color:Tc"

# start window numbers from 1
set -g base-index 1

# current path as a window name
set-option -g automatic-rename on
set-option -g automatic-rename-format "#{b:pane_current_path}"

# reduce escape time
set-option -g escape-time 10

# renumber windows
set-option -g renumber-windows on

# Current path as window name
set-option -g automatic-rename on
set-option -g automatic-rename-format "#{b:pane_current_path}"

set -gw window-status-current-format ' #I#[fg=colour250]:#[fg=colour255]#W#[fg=colour2]#F '
set -gw window-status-format ' #I#[fg=colour237]:#[fg=colour250]#W#[fg=colour244]#F '

set -g status-left ''
set -g status-right ''

set -g status-bg colour234
set -g status-fg colour137
